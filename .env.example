# Copy to .env.local (and set the same two in Vercel) to switch the /admin
# fee system from device mode to Supabase. See supabase/schema.sql + README.
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-public-key
